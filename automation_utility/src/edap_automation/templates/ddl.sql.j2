CREATE TABLE {{ table_name }} (
{% for col in columns %}
  `{{ col.name }}` {{ col.datatype }}{{ ' GENERATED BY DEFAULT AS IDENTITY' if col.identity }}{{ ' NOT NULL' if not col.nullable else ' NULL' }}{% if not loop.last %},
  {% endif %}
{% endfor %}

) USING DELTA;
